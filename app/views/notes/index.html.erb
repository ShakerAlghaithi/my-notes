 <div style="position:fixed; right:70px;top:80px; color:red;">
    <%= link_to "Connect to Google calendar", "/auth/google_oauth2" %>
</div>
  <nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container-fluid">
       <a  style="text-shadow:4px 4px 8px red ;" class="navbar-brand" href="#">MY NOTES</a>
       
       <%= link_to "Sign-out", signout_path, :method => :delete, :class => "btn btn-danger btn-sm navbar-right"%>
               
   </div>
  </div>
 </nav>
 <table id="table", class="table">
    <tr> 
      <th>NOte Title</th>
     </tr>
       <% @notes.each do |note| %>
       <tr>
          <td><%=link_to note.title, note_path(note)%></td>
          <td><%=link_to"Delete", note, method: :delete, data: {confirm: "Are you sure?"}%></td>
       </tr>  
     <% end %>
 </table>
   <%= link_to "New_note", new_note_path, :class => "btn btn-success btn-sm"%>


